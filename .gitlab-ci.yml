stages:
    - build
    - test

build website:
    image: node:16-alpine
    stage: build
    script:
        - cd frontEnd
        - cd carbooking
        - yarn install
        - yarn build
    artifacts:
        paths:
            - build

test website:
    image: alpine
    stage: test
    script:
        - ls

unit test:
    image: node:16-alpine
    stage: test
    script:
        - yarn install
        - yarn test


